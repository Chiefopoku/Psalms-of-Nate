# Deployment

Set Firebase public environment variables before deployment. The app is ready for Vercel or any Next.js-compatible host.

Admin routes are mounted under `/admin/*`; configure authentication before publishing private workflows.
